import {StyleSheet} from 'react-native';

export const CheckboxStyles = StyleSheet.create({
  itemContainer: {paddingHorizontal: 5, paddingBottom: 15},
  iconOuter: {borderRadius: 0, borderWidth: 2},
  iconInner: {borderRadius: 0, borderColor: '#000'},
  title: {
    fontFamily: 'poppinsRegular',
    color: '#000',
    fontSize: 14,
    lineHeight: 21,
    textDecorationColor: '#FFD615',
  },
});
